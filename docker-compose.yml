services:
  python:
    build: .
    stdin_open: true # docker run -i
    tty: true        # docker run -t
    command: sh -c "tail -f /dev/null"
    ports:
      - 8000:8000
    volumes:
      - .:/usr/src/app
